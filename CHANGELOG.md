## [Unreleased]

### 🚀 Added
- **视频观看历史记录**：新增本地视频播放历史保存（使用 `chrome.storage.local`，上限 90 条），自动记录播放视频的 URL 链接、网页标题以及具体观看日期与时间。
- **Popup 历史记录面板**：Popup 弹窗新增「观看历史」Tab，支持快速查看、新标签页一键访问、单条删除及一键清空全部历史。

---

## [1.3.0] - 2026-08-06

### 🚀 Added
- **字幕系统支持**：引入外挂字幕解析器 (`subtitles/parser.js`) 与实时渲染器 (`subtitles/renderer.js`)。
- **字幕自定义样式设置**：Popup 设置面板增加字幕字体大小、字体颜色、背景颜色、背景透明度、字重及底部偏移量调节功能。
- **自定义快捷键**：支持自定义快进与快退的触发按键（默认 `J` / `L`）。

### 🐛 Fixed
- 修复 Vimium / Vim 等浏览器按键扩展截获 `J`/`L`/`ESC` 事件的问题（在事件捕获阶段使用 `stopImmediatePropagation`）。

---

## [1.2.0] - 2026-08-05

### 🚀 Added
- **Popup 设置面板**：新增独立设置弹窗界面 (`popup.html` / `popup.js`)。
- **时间步长设定**：支持独立设定 J/L 键快进/回退的时间数值与单位（秒/分/时）。
- **遮罩透明度设置**：支持自定义影院遮罩层透明度 (0.0 - 1.0)。

### 🐛 Fixed
- 修复输入框/可编辑区域 (`contenteditable`) 内按键误触发快进快退的问题。

---

## [1.1.0] - 2026-08-04

### 🚀 Added
- **6向快进/回退控制条**：底部中央新增悬浮控制工具栏，提供 `-10m` / `-1m` / `-10s` / `+10s` / `+1m` / `+10m` 极速定位按钮。

### 🐛 Fixed
- 修复影院模式下视频元素的居中布局计算、尺寸重绘和回退兜底逻辑问题。

---

## [1.0.0] - 2026-08-01

### 🚀 Added
- **初始版本上线**：
  - 自动检测网页 HTML5 `<video>` 视频元素并展示一键影院模式悬浮按钮。
  - 暗黑全屏遮罩及全屏剧场沉浸式播放。
